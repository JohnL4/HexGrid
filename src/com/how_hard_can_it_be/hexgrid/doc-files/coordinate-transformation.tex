\documentclass{letter}
\usepackage{amsmath}

%%%%%%%%%% Start TeXmacs macros
\newcommand{\tmop}[1]{\ensuremath{\operatorname{#1}}}
\newcommand{\tmtexttt}[1]{{\ttfamily{#1}}}
%%%%%%%%%% End TeXmacs macros

\begin{document}

We want an easy conversion from hex-space to square-space, so we can convert a
mouse click to a hex, and a hex to x-y coordinates.

See this paper: \tmtexttt{http://www.math.ucsd.edu/\~{ }nslingle/bases.pdf}

Transform from ($\overrightarrow{u_1}$, $\overrightarrow{u_2})
\tmop{to}$($\overrightarrow{x_1}, \overrightarrow{x_2}) :$
\[ \vec{x} = W \vec{u} \]
Columns of $W$are base vectors of $u$-space, expressed in $x$ coordinates:



\end{document}
